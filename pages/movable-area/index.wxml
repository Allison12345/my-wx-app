<my-page>
  <movable-area class="my-area">
    <movable-view
      class="my-view"
      x="{{ x }}"
      y="{{ y }}"
      direction="all"
      scale="{{ true }}"
      scale-max="5"
      scale-min="1"
    >
    </movable-view>
  </movable-area>
</my-page>
