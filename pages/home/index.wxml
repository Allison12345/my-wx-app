<my-page>
  <home-pane
    wx:for="{{ list }}"
    wx:key="label"
    label="{{ item.label }}"
    items="{{ item.items }}"
    isOpen="{{ index === selectedIndex }}"
    index="{{ index }}"
    bind:changeIndex="changeIndex"
  ></home-pane>
</my-page>
